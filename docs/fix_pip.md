# BROKEN PIP ENVIRONMENT FIX		

$> sudo apt install --reinstall python3-venv python3-pip python3-virtualenv

$> python3 -m ensurepip --default-pip

$> python3 -m pip install --upgrade pip

$> pip list
